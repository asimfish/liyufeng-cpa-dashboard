# CPA Dashboard

This directory is a static GitHub Pages frontend. It intentionally contains no
CPA key, OAuth credential, proxy URL, or turn-state value. Configure the page
with `?api=https://your-read-only-api/control/v1` and enter a separately issued
read-only dashboard token in the page.
